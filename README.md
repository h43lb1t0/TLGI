A simple base converter that can handle conversions between various numeral systems (2 - 10 and 16).

# Usage
To use the base converter, run the script with the following command:

```bash
python BasenWechsler.py
```
